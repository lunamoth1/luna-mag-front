import styles from "./contactsPage.module.css";
import { JSX } from "react";

export default function ContactsPage(): JSX.Element {
	return (
		<div className={styles.main}>
			<p className={styles.text}>Contacts Page</p>
		</div>
	);
}
