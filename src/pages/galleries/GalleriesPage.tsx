import styles from "./galleriesPage.module.css";
import { JSX } from "react";

export default function GalleriesPage(): JSX.Element {
	return (
		<div className={styles.main}>
			<p className={styles.text}>Galleries Page</p>
		</div>
	);
}
